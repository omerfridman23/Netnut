# Base URL the frontend uses to call the API. Defaults to "/api" (same origin),
# which works both in dev (Vite proxy) and prod (nginx proxy to the gateway).
VITE_API_BASE_URL=/api

# Wallet balance (cents) at or below which a "low balance" warning is shown.
VITE_LOW_BALANCE_THRESHOLD_CENTS=500
